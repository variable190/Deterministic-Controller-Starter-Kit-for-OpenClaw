### Sprint Plan

**Project:** `<PROJECT NAME>`
**Sprint state:** `PAUSED`
**Spec:** `<path/to/spec.md>` (if applicable)

**Goal (one sentence):**
`<What "done" means>`

**Non-goals:**
- `<Explicitly excluded work>`

**Assumptions / Constraints:**
- `<e.g., research-only, no external actions, requires no credentials>`

#### Definition of Done (evidence-gated)
- [ ] `<Deliverable>` — Evidence: `<path/to/artifact>`
- [ ] `<Deliverable>` — Evidence: `<path/to/artifact>`

---

#### Step table

Every step must be executable with zero context. Include exact file paths, commands, and completion conditions.

| # | Step (action + completion condition) | Status | Evidence/Output |
|---|---|---|---|
| 1 | Create `<file>` with sections: `<A, B, C>`. Done when file exists with all sections. | TODO | `<path>` |
| 2 | Implement `<component>`. Done when `<command>` exits 0. | TODO | `<path>` |
| 3 | Integration test: run `<command>`. Done when output contains `<expected>`. | TODO | `<path/to/output>` |
| 4 | Write closure note (what was built, how to run, known limitations). | TODO | `<path>/closure.md` |

---

#### BLOCKED (only if human action required)

| Item | Blocker | Unblock Condition | Owner |
|------|---------|-------------------|-------|
| (none) | - | - | - |
